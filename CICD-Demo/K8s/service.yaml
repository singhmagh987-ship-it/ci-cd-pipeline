apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-boot-products
  name: spring-boot-products
  namespace: default
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: spring-boot-products
  sessionAffinity: None
  type: LoadBalancer
status:
  loadBalancer: {}
