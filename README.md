Creating a sample Spring boot application which return the product list and the application gets build and deployed to the Kubernetes(EKS) through a CI/CD Pipeline using Github Actions.

Application --> Run Unit Tests --> Build the application --> Build Docker Image --> Publish the Docker Image to Docker Hub --> Deploy the image to AWS EKS Cluster.
